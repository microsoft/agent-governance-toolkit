# agent-mcp-governance compatibility shim

This directory remains as a packaging compatibility shim for existing monorepo workflows.

The canonical package home now lives at:

- `agent-governance-python/agent-mcp-governance/`

Installing from this legacy path packages the real `agent_mcp_governance` source from that
top-level package home instead of duplicating the code here.
