<Project>
  <!-- Evaluated after project properties, so IsPackable is available -->
  <PropertyGroup Condition="'$(IsPackable)' != 'false'">
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
</Project>
