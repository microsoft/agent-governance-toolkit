# openshell-agentmesh

This compatibility package is deprecated. Install the maintained integration
instead:

```bash
pip install "agent-governance-toolkit-integrations[openshell]"
```

The maintained integration consumes native ACS runtime configuration and does
not load a framework-local rule format.
