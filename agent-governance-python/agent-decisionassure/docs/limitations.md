# Limitations

Replay establishes only what the supplied artifacts support. Missing data can be unknown; risk estimates depend on supplied business values. Hashes detect integrity changes, not identity. Policy semantics are limited to the documented DSL. External behavior and environment equivalence cannot be assumed, and historical traces can be incomplete.
