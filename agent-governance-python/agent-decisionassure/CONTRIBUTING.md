# Contributing

Keep changes deterministic and offline. Add a regression test for behavior changes, never add dynamic policy execution, and run `python -m pytest` from this project before submitting a change.
