agent_control_specification_version: "0.3.0-alpha"
extends:
  - base.yaml
policies:
  shared_policy:
    type: custom
    adapter: different_adapter
intervention_points:
  output:
    policy_target: "$snap.output"
    policy:
      id: shared_policy
