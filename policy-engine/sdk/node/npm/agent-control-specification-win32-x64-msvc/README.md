# agent-control-specification-win32-x64-msvc

Native Node.js binding used by `agent-control-specification` on Windows x64 MSVC.
The `.node` binary is produced by the release pipeline and is not committed.
