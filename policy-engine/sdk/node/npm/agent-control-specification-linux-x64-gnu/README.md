# agent-control-specification-linux-x64-gnu

Native Node.js binding used by `agent-control-specification` on Linux x64 glibc.
The `.node` binary is produced by the release pipeline and is not committed.
